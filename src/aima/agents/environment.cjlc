(ns aima.agents.environment)


(defprotocol Agent
  (execute [agent percept]))

(defprotocol Environment
  (perceive [env sensor-fn])
  (actuate [env action])
  (step [env])
  (run [env])
  (done? [env]))
